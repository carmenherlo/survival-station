# Python
__pycache__/
*.py[cod]
*.egg-info/
*.egg

# Environment
.env

# Conda environments (not needed — everything runs in Docker)
env_base_requirements.yml
environment.yml

# Editor
.vscode/

# Internal — not for repo
notes/
lab/

# Models (too large for git)
data/models/

# Offline content (too large for git)
data/wiki/
data/translate/

# Docker compose override (use GPUs locally, but not in production)
docker-compose.override.yml

# TLS certificates (machine-specific, never commit)
pwa/certs/

# OS
.DS_Store