server {
    listen 80;

    # Serve PWA
    location / {
        root /usr/share/nginx/html;
        index index.html;
    }

    # Proxy API requests to rag-api container
    location /api/ {
        proxy_pass http://rag-api:8000/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # Offline maps
    # location /maps/ {
    #     proxy_pass http://tileserver:8080/;
    #     proxy_set_header Host $host;
    #     proxy_set_header X-Real-IP $remote_addr;
    # }

    # Offline wiki
    # location /wiki/ {
    #     proxy_pass http://kiwix:8888/;
    #     proxy_set_header Host $host;
    #     proxy_set_header X-Real-IP $remote_addr;
    # }

    # Offline translator
    # location /translate/ {
    #     proxy_pass http://libretranslate:5000/;
    #     proxy_set_header Host $host;
    #     proxy_set_header X-Real-IP $remote_addr;
    # }
}